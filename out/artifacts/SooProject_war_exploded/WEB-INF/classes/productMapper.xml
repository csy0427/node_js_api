<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product">

    <select id="listProduct" resultType="com.spring.Shop.dto.ProductVO">
        SELECT
        product_id AS productId
        , product_name AS productName
        , product_price AS productPrice
        , product_desc AS productDesc
        , product_url AS productUrl
        FROM tbl_product ORDER BY product_name
    </select>

    <select id="detailProduct" resultType="com.spring.Shop.dto.ProductVO">
        SELECT
        product_id AS productId
        , product_name AS productName
        , product_price AS productPrice
        , product_desc AS productDesc
        , product_url AS productUrl
        FROM tbl_product
        WHERE product_id = #{productId}
    </select>

    <insert id="insertProduct">
        INSERT INTO tbl_product
        VALUES (
          NEXTVAL('seq_product')
        , #{productName}
        , #{productPrice}
        , #{productDesc}
        , #{productUrl}
        )
    </insert>

    <update id="updateProduct">
        UPDATE tbl_product
        SET product_name = #{productName}
        , product_price = #{productPrice}
        , product_desc = #{productDesc}
        , product_url = #{productUrl}
        WHERE
        product_id = #{productId}
    </update>

    <delete id="deleteProduct">
        DELETE FROM tbl_product
        WHERE product_id = #{productId}
    </delete>

    <select id="fileInfo" resultType="String">
        SELECT product_url AS productUrl
        FROM tbl_product
        WHERE product_id = #{productId}
    </select>
</mapper>